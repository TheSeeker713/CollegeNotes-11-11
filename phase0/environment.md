# Step 0.1 — environment and minimal bootstrap

Actual Darwin/macOS execution and a native Developer directory were verified. The resolved project is the CollegeNotes child of that directory, absent before creation. Existing sibling projects were inspected and preserved. Detailed hardware, absolute paths, tool versions and command outputs are private in `.local/verification/environment.json`.

Display name: CollegeNotes{11:11}. Repository: TheSeeker713/CollegeNotes-11-11, public source only. GitHub `/user` confirms the requested owner (case-insensitive match). Exact slug availability was checked before creation; successful GitHub creation is the final naming validation. Repository-local commit identity uses the authenticated public account name and numeric-ID GitHub no-reply email; no global Git configuration changes.

No product dependencies, scaffold, features, or model downloads are part of this step. Installed system tools are inventory, not a project stack selection or compatibility test. Graphics capability and application performance remain untested.

Read-only inventory initially encountered sandbox restrictions: directory-service lookup failed; canonical home was resolved from the actual existing filesystem/user context instead. GitHub auth failed without network permission and succeeded after permission was granted. The hardware profiler emitted a CPU-family warning while returning the model/chip/memory inventory; no application benchmark is inferred from it.

Required checks fixed before checkpoint: C01 native machine evidence; C02 canonical destination and preservation; C03 staged privacy and minimal scope; C04 authenticated owner, slug and local identity; C05 local document/source audit; C06 commit/push/remote identity. C01–C05 must pass before committing; C06 completes the checkpoint. No application tests apply.
