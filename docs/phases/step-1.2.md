# Step 1.2 — local gate

Scope and required checks: phase manifest/obligations for this step. 4/4 checks passed; no failed, missing or skipped cases.

- all_guides_linked: All 8 required guides plus instruction-loading checklist exist, are substantive and linked from root
- instruction_loading: Official discovery documentation fetched/read; explicit current-session loading distinguished from future automatic startup
- workflow_scenarios: Separate written scenario walkthrough: fail→repair; push failure→pending; boundary→actual authority; defect→same phase; lost context→state/evidence inspection
- user_authority_only: Authority is actual user message; no agent, validator or GitHub acceptance invented

Separate audit: Distinct pass inspected all guide content, links and exception handling against the current user message and full plan. Executable state rehearsals follow in 1.4; no automated tool hot-loading claim.

This precommit record requires a successful checkpoint. Exact commands, checked source tree and remote receipt are in ignored .local/verification/current-checkpoints.json. A later step or closeout records the actual commit; no self-referential future SHA is asserted. No application tests are claimed.
