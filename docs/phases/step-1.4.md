# Step 1.4 — local gate

Scope and required checks: phase manifest/obligations for this step. 4/4 checks passed; no failed, missing or skipped cases.

- negative_transition_cases: 42/42 actual synthetic rule cases passed, including failed/missing/skipped checks, rejected transitions, failed push, forbidden Phase 3 and closed pass
- synthetic_separation: Synthetic fixtures are labeled and rejected by the real-evidence gate; no product results asserted
- scaffold_regression: Planning validation, all scaffold checks and all 60 manifest source comparisons passed against current files
- phase_audit_and_handoff: Separate phase audit reconciled guide rules, 28 requirements, all step artifacts, ordered prior trees/remote receipts, privacy and next-phase exception; review/setup/testing handoffs exist

Separate audit: Pass: every required Phase 1 obligation accounted for, actual reports read and source hashes recomputed; no installed product or fabricated approval. Final step remote receipt remains checkpoint obligation.

This precommit record requires a successful checkpoint. Exact commands, checked source tree and remote receipt are in ignored .local/verification/current-checkpoints.json. A later step or closeout records the actual commit; no self-referential future SHA is asserted. No application tests are claimed.
