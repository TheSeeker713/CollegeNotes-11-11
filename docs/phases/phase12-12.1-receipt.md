# Phase 12.1 — visual-aid contract and process sequence

AUTH-P12 / PASS-2026-09-18-P12. CHK-12.1-01 through CHK-12.1-06 are covered by `tests/integration/visuals-sequence.test.ts`: state/action fixtures, labels/units, keyboard equivalents, serialize/restore, source links, and predictable clamped transitions.

Domain visual-aid API (`packages/domain/src/visuals.ts`), `visuals` module enabled (`available: true`), storage migration `visual_experiments`, local-service routes, Visuals workspace, theme `visuals` tokens, instructional aid package helpers, AUTH-P12, and AGENTS Phase 12 authorization are included. Course-neutral only — no COMM 110, syllabus, or assignment content. Decorative GlassAtmosphere remains separate from instructional aids. Owner UI/UX acceptance remains pending.

Non-UI gate: types, lint, build, 32 unit, 256 integration, 2 evaluation, 6 planning, 44 workflow. Private evidence: `.local/verification/phase12/12.1/`.

Verified origin/main checkpoint: _(filled after push)_.
