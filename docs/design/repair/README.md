# Revised desktop design candidate

[PROJECT-PLAN.md](../../../PROJECT-PLAN.md) controls scope. This candidate supersedes class-specific assumptions in previous prompts/prototypes while retaining approved theme tokens and the 35 private image references as visual direction. Old artifacts remain historical, not production content.

Open [design gallery](index.html). Fifteen labeled state panels cover empty courses, course management, archive/restore, destructive confirmation, material import/correction/export/deletion, Connections, authentication, capability/default/billing controls, removal, research/provenance, offline/recovery, reading/study. The gallery is a reviewable design specification, not a working future-phase application. Expand each recovery disclosure; use both themes and both modes.

Keep [desktop interaction specifications](../04-accessibility-and-behavior.md): semantic headings, 44px targets, keyboard/focus return, default cancel for destruction, opaque readable backings, no motion-dependent information, 1440×900 reference, 1024×700 minimum, 200% zoom. Confirm dialog focus, error announcements and keyboard behavior manually once those features are implemented. No agent may run UI/UX tests or inspect rendered output for acceptance in this pass.

Owner review is pending. Source audit confirms state coverage and synthetic labeling only; it does not prove usability, rendering or accessibility. The owner explicitly requested all repair implementation through Phase 4 before manual UI/UX testing. No design acceptance is inferred from authorization to execute.
