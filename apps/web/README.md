# apps/web

React browser interface. Own navigation, rendering and accessible user interactions; use domain interfaces and local-service requests. No credentials or authoritative filesystem storage in the browser.

Scaffold only. No application features or installed product dependencies exist.
