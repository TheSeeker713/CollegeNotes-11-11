# packages/storage

Authoritative SQLite migrations and file metadata, transaction boundaries, backups and restoration. Private application data stays outside the checkout.

Scaffold only. No application features or installed product dependencies exist.
