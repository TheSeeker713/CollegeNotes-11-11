# tests/evals

Versioned source-grounding and course-policy cases with explicit semantic rubrics. Passing cases never mean the model cannot fail.

Scaffold only. No application features or installed product dependencies exist.
